{
  "type": "object",
  "required": ["id", "name", "version", "components"],
  "properties": {
    "id": { "type": "string" },
    "name": { "type": "string" },
    "version": { "type": "string" },
    "components": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "type"],
        "properties": {
          "id": { "type": "string" },
          "type": { "type": "string", "enum": ["input", "transform", "output"] },
          "contracts": {
            "type": "array",
            "items": { "type": "string" }
          }
        }
      }
    }
  }
}

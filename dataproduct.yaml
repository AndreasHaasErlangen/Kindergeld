schema: https://opendataproducts.org/v4.0/schema/odps.yaml
version: 4.0
product:
  details: 
    de:
      name: Kindergeld
      productID: famka-kindergeld
      productVersion: '0.1.0'      
      visibility: invitation
      status: draft
      type: dataset
      valueProposition: Beschreibung des Mehrwerts die dieses Datenprodukt bringen soll. 
      description: Dieses Datenprodukt stellt aggregierte Kindergeld-Bestandsdaten bereit, die für Controllingauswertungen verwendet werden können.
  contract:
    # Optionally se URI of external file, contains all contract details
    $ref: 'https://example.org/contracts/default.yaml'
    # Or then define with attributes
    id: bestandsdaten-kg-estg-v1.0.0  
    type:  ODCS 
    contractVersion:  0.1.0
    contractURL: ./contracts/bestandsdaten-kg-estg-v1.0.0.yaml
    spec:
        ....